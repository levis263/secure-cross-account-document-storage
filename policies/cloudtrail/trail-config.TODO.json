{
  "_comment": "PLACEHOLDER — do not commit as real config. Replace once the trail is actually created in the console. Intended settings: trail name project1-document-access-trail, log file validation enabled, management events (Read+Write) captured, S3 data events scoped ONLY to project1-sensitive-data (not account-wide, to keep cost bounded). Deliberately excludes the trail's own logging bucket from being the same bucket it audits.",
  "TrailName": "REPLACE",
  "S3BucketName": "REPLACE — separate logging bucket, not project1-sensitive-data",
  "EnableLogFileValidation": true,
  "IsMultiRegionTrail": "REPLACE",
  "EventSelectors": {
    "ManagementEvents": "ReadWrite",
    "DataEvents": {
      "S3": {
        "BucketName": "project1-sensitive-data",
        "ReadWriteType": "REPLACE"
      }
    }
  }
}
